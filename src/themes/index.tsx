// import { Theme } from "theme-ui";
// import { LightTheme } from "./LightTheme";

// const makeTheme = <T extends Theme>(t: T) => t;

// const light = makeTheme(LightTheme);

const SiteThemes = {
	// light,
};
export default SiteThemes;
