import { SocialItems } from "../../data/SocialItems";
import { PureSocialLinkList } from "./SocialLinkList";

const SocialLinkList = () => {
	return <PureSocialLinkList items={SocialItems} />;
};

export default SocialLinkList;
